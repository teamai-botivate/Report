# Base URL of the backend API. In local dev (separate `next dev` / `uvicorn`
# processes) point this at the backend's own port. In the single-container
# Docker/Render deployment, leave this unset (or empty) so requests go to the
# same origin, since FastAPI proxies the frontend and serves /api/* itself.
NEXT_PUBLIC_API_URL=http://localhost:8000
